{
    "buildCommand": "npx expo export -p web",
    "outputDirectory": "dist",
    "framework": "expo",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}